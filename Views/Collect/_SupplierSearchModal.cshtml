<div class="modal fade" id="searchSupplierModal" tabindex="-1" aria-labelledby="searchSupplierModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content" style="height: 600px; max-height: 90vh;">
      <div class="modal-header">
        <h5 class="modal-title" id="searchSupplierModalLabel">Selecione o fornecedor</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
      </div>
      <div class="modal-body" style="overflow-y: auto;">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th colspan="3">
                            <div class="d-flex justify-content-center mt-1 mb-2">
                                <input type="text" id="searchSupplierInput" 
                                       class="form-control" 
                                       placeholder="Digite para buscar fornecedor..." 
                                       style="max-width: 300px;">
                            </div>
                        </th>
                    </tr>
                    <tr>
                        <th>Id</th>
                        <th>Fornecedor</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="suppliersList">
                </tbody>
            </table>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
      </div>
    </div>
  </div>
</div>
