@model int

<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Deletar coleta</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
      </div>
      <div class="modal-body">
        Deseja realmente deletar a coleta de id: @Model ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
        <form asp-controller="Collect" asp-action="DeleteCollect" method="post">
            <input name="id" asp-for="@Model" type="hidden" value="@Model">
            <button class="btn btn-danger">
                Deletar
            </button>
        </form>
      </div>
    </div>
  </div>
</div>